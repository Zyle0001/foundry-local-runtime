# Local Models Folder

Put local model folders here, for example:

- `ONNX host service/models/Whisper/...`
- `ONNX host service/models/Kokoro/...`

This folder is ignored by Git except for this file and `.gitkeep`.
